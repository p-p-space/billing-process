# enviaronment
WEB_ENV={{ local | dev | test | uat | prod }}

# server env only for local settings
WEB_URL={{WEB_URL}} # url for application access
SERV_URL={{SERV_URL}} # url for api access
TIMEZONE={{TIMEZONE}} # Time zone for logger and other services

# Cookie env only for local settings
COOKIE_EXPIRES={{COOKIE_EXPIRES}}

# Redis env only for local settings
REDIS_HOST={{REDIS_HOST}}
REDIS_PORT={{REDIS_PORT}}
REDIS_DB={{REDIS_DB}}
REDIS_SSL={{ ON | OFF}}
REDIS_USER={{REDIS_USER}}
REDIS_PASSWORD={{REDIS_PASSWORD}}

# session env only for local settings
SESS_EXP_TIME={{SESS_EXP_TIME}}

# credentials env only for local settings
TENANT_ID={{TENANT_ID}}
TENANT_CLIENT_ID={{TENANT_CLIENT_ID}}
TENANT_CLIENT_SECRET={{TENANT_CLIENT_SECRET}}
COGNITO_REGION={{COGNITO_REGION}}
COGNITO_USER_POOL_ID={{COGNITO_USER_POOL_ID}}
COGNITO_CLIENT_ID={{COGNITO_CLIENT_ID}}
COGNITO_CLIENT_SECRET={{COGNITO_CLIENT_SECRET}}